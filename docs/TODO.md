# ToDo List

> **進捗管理用・設計は検討中（Draft）** — このリストに沿って作業を進めます。未完了の項目や「提案」は、確定した仕様・実装済みの機能を意味しません。

更新日: 2026-08-31

**現在地:** 資料整理と実機での短いメモリ確認まで完了。設計の差分は下記に整理済みで、実装・実データでの学習は未着手です。**次の項目は D-01〜D-06（Phase 1に必要な設計調整）です。**

[ドキュメント案内](README.md) / [実験計画](EXPERIMENT_PLAN.md) / [実装仕様案](IMPLEMENTATION_SPEC.md) / [参考文献](REFERENCES.md)

## 進め方

- 上から順に進めます。各段階の完了条件を満たしてから、依存する次の段階へ進みます。
- ユーザーが仕様・優先順位を決定し、Agentがその範囲の文書・コードを作成、レビューします。実装の完了と、実行による検証の完了は分けて記録します。
- プログラム・テスト・学習・解析の実行は [AGENTS.md](../AGENTS.md) に従ってユーザーが担当します。Agentによる実行が個別に許可された場合は、その許可範囲で進めます。環境・B256確認に対する既存の許可を、依存パッケージの変更や本学習全体への許可には拡張しません。
- 完了時は `[ ]` を `[x]` にし、成果物または実行結果の所在を添えます。差が出なかった実験や、精度が改善しなかった実験も、手順と記録が成立していれば完了として扱います。
- 数値、データ分割、モデル、評価方法の変更は黙って行いません。決定後に実験計画・実装仕様・設定を揃えます。

## 0. 完了済みの準備

- [x] P-01: 概要をルート、詳細資料を`docs/`、設定例を`docs/examples/`へ整理し、Draftであることを明記。[資料案内](README.md)
- [x] P-02: 提供された18件と補足8件の参考資料を、用途とともに記録。[参考文献一覧](REFERENCES.md)
- [x] P-03: GPU・RAM・Python環境を確認し、224×224・B256・bf16・AdamWで合成入力による3回の更新を実施。ピークallocatedは約8.65 GiB。[測定条件と限界](IMPLEMENTATION_SPEC.md#21-environment-check-2026-08-31)
- [x] P-04: 既存草案と会話中の設計提案の差分・未決定事項を、このリストに整理。

P-03は、事前学習重みの読み込み、CIFAR-10での学習、精度、収束、再開の検証を済ませたことにはなりません。

## 1. Phase 1に必要な設計調整

担当: ユーザーが選択・決定、Agentが整合確認と文書化。

- [ ] D-01: Phase 1の最小範囲と共通条件を確定する。
- [ ] D-02: checkpointの保存間隔、時間軸、観察できる粒度を確定する。
- [ ] D-03: PCAの原点・方向・対象checkpoint・メモリ方針を確定する。
- [ ] D-04: 損失平面の評価データ・前処理・精度と、実測損失の表示方法を確定する。
- [ ] D-05: 設定・保存形式・パス・実験予算・完了条件を揃える。
- [ ] D-06: 決定した範囲を[実験計画](EXPERIMENT_PLAN.md)、[実装仕様案](IMPLEMENTATION_SPEC.md)、[設定例](examples/config_example.yaml)へ反映し、相互の矛盾を解消する。後段の未決定事項はDraftとして残す。

| 対象 | 現状・未決定事項 | 会話中の提案・検討方向（未確定） |
| --- | --- | --- |
| D-01: 実験範囲 | 草案はCIFAR-10、DINO ViT-S/16、224×224、full fine-tuning、AdamW。B16/B64/B256 × 3 seedsを想定。 | 最初はseed 0の3バッチ比較を完成させ、その後seed 1・2を追加。同じ分類headまで含む初期checkpointを共有。 |
| D-01: 学習条件 | 50 epochs、LR `1e-4`、weight decay `0.05`、warmup 5 epochs、bf16は候補値。 | Phase 0で成立性を確認した後、本比較前に固定。runごとの都合で調整しない。 |
| D-02: 保存間隔 | 実験計画は初期0.5 epoch・後半1 epoch、設定例は1 epoch、型定義案はstep単位。 | 保存イベントの定義を統一し、実際のepoch・optimizer stepも記録。粗いcheckpoint間を結んだ線から更新単位の揺らぎを主張しない。 |
| D-03: PCA | 原点とPCAの中心化の対応、対象checkpointの選び方が未定。 | 同一比較内で共通の平均・直交基底を使用。runごとに同じepoch間隔で選び、寄与率と射影残差を保存。 |
| D-03: メモリ | RAMは約32 GiB。全runの重みを一括展開する余裕は前提にできない。 | 全checkpointのRAM/GPU常駐を避ける方式を選び、PCAの作業領域も見積もる。 |
| D-04: 背景損失 | 草案はvalidation由来の固定1,000件。会話ではtrain由来を主表示、validationを補助表示とする案を提示。 | 採用するsubsetと表示の役割を決める。背景の平面上の損失と、実モデルの損失を区別する。 |
| D-05: 設定・再開 | YAMLは階層型、dataclass例は平坦。再開時の乱数状態・データ順序の契約が不足。 | 設定の階層を揃え、相対パスはプロジェクトルート基準にする。再開用と解析用checkpointを分離し、必要な状態を明示。 |
| D-05: 予算 | B256の短いメモリ確認のみ完了。実データでの所要時間、成果物全体の容量は未確認。 | 許容時間・保存容量を決め、Phase 0の実測から本実験の実施規模を見積もる。既存ファイルを自動削除しない。 |
| D-05: 完了条件 | 草案のPhase 1終了条件には、軌跡差の観察と、後段で実装する補間評価が混在。 | Phase 1は学習・記録・共通座標・動画の成立で判定。補間評価はPhase 2の準備で実装・検証し、実験結果の好悪を進行条件にしない。 |

完了条件: Phase 1の実装に必要な判断が文書上で揃い、実装時にデータや数値の意味を推測する必要がない。

## 2. 学習・保存の基盤を実装

依存: D-01〜D-06。担当: Agentが実装・レビュー、ユーザーが検証コマンドを実行。

- [ ] I-01: 決定した最小構成だけを作成し、設定読み込み・型・入力検証・パス解決・実行設定の保存を実装。既存`torch_env`を起点に必要な依存関係と動画出力手段を整理し、環境を無断で変更しない。
- [ ] I-02: CIFAR-10の読み込み、共有train/validation split、前処理、seed管理を実装。testを選択・調整に使わない境界を設ける。
- [ ] I-03: 事前学習モデルの読み込みと10-class headの一度だけの初期化、共通`theta_0`の保存・読み込みを実装。
- [ ] I-04: 学習・評価・LR schedule・実測ログ・解析用checkpoint・再開用checkpointを実装。評価のための処理が学習の乱数列を意図せず変えないよう扱いを定義する。
- [ ] I-05: 採用した検証範囲で、設定の不正値、初期値の共有、checkpoint復元、決定的な評価、再開状態に関する検証を用意してレビューし、実行結果を確認。

完了条件: 決定済みの設定で学習・保存・評価を呼び出せるコードと実行手順が揃い、未検証箇所が明記されている。

## 3. Phase 0: 実データによる短い確認

依存: I-01〜I-05。実行担当: ユーザー（Agent実行の明示的な許可がある場合はその範囲）。

- [ ] S-01: データと事前学習重みを用意し、共有splitと初期checkpointを生成。取得・生成したものの所在を記録。
- [ ] S-02: B64・seed 0・5 epochsを候補とするsanity runを実行し、loss・accuracy・VRAM・所要時間・保存容量を確認。
- [ ] S-03: 保存・読み込み後の評価、再開、実データでのメモリを確認し、本比較に使う設定と実験予算を固定。失敗時は原因を修正して再確認する。

完了条件: 実データで学習・評価・保存が成立し、再開と資源見積もりが確認できる。可視化の短い確認は次のV-05で行う。

## 4. 射影・損失平面・アニメーションを実装

依存: 学習・保存基盤とPhase 0の成果物。担当: Agentが実装・レビュー、ユーザーが実行。

- [ ] V-01: 重みのベクトル化と復元、parameterの順序・shape・dtype・bufferの扱いを実装し、roundtripを検証。
- [ ] V-02: 共通PCA、座標、寄与率、射影残差を生成・保存。比較対象を追加してPCAを作り直す場合は別の識別子で保存し、異なる座標系を混在させない。
- [ ] V-03: 決定した固定subsetによる21×21の損失平面を事前計算・保存。評価設定と対象subsetを追跡可能にする。
- [ ] V-04: 固定した背景・軸・色尺度に、履歴・現在点・epoch・step・LR・実測指標を表示する動画生成を実装。再描画時はモデル評価を行わない。
- [ ] V-05: Phase 0のcheckpointから短い動画を生成し、時間軸、実測値、座標、保存済み成果物からの再描画を確認。MP4出力、または利用できない場合のGIF出力を確認。

完了条件: 保存済みの座標・格子・指標だけで動画を再生成でき、背景損失と実測損失の意味が表示から区別できる。

## 5. Phase 1: 最初の成果物を完成

依存: S-03、V-01〜V-05。

- [ ] M-01: 共通`theta_0`からseed 0のB16/B64/B256を実行し、同じ時間軸・共通座標で比較する動画を完成させる。
- [ ] M-02: ログ、保存間隔、学習条件、射影残差、動画表示を点検し、最小版の完了を確認。
- [ ] M-03: seed 1・2を追加して全9 runsを揃え、共通PCAを再計算。seedごとの動画、全runのsummary動画、条件別の評価表を保存。

最小版の完了条件: seed 0の3バッチ比較動画と、その元となる設定・checkpoint・実測ログが揃う。

Phase 1の完了条件: 予定した全9 runsの記録と動画が揃い、再現手順が明記される。バッチ条件による差が出ること自体は必須にしない。

## 6. Phase 2: 補間・SWA・FGE

依存: Phase 1の完了。ここから先の条件は、この段階の着手前に確定する。

- [ ] A-01: B64・seed 0・epoch 30から分岐する案、共通のoptimizer/乱数状態、Normal/SWA/FGEのLR、平均開始、採取時点、評価対象を確定。
- [ ] A-02: 高次元の重みを直接線形補間する評価を実装し、端点・同一モデル・有限な評価点でのbarrier定義を検証。初期案は21点。2D表示からbarrierを推定しない。
- [ ] A-03: SWAのraw trajectoryとrunning average、FGEのsnapshotと確率平均ensembleを実装し、平均前・平均後・予測平均を別々に評価できるようにする。
- [ ] A-04: 共通分岐点から実行し、同じ座標系でのNormal/SWA/FGE動画、SWA平均点の動画、補間結果、性能比較を保存。

現時点の候補: SWAはepoch 35〜50の平均、FGEは4 epoch周期で最低LRとなるepoch 32/36/40/44/48を採取。これらは実装前にA-01で確定する。1 seedだけの結果をseed間で一般化しない。

完了条件: 学習率操作と平均化を区別して比較でき、動画・評価値・使用checkpointを追跡できる。精度改善は必須にしない。

## 7. Phase 3: 複数runとModel Soup

依存: Phase 2と補間・平均化の検証完了。

- [ ] U-01: 候補モデルの集合・epochと、Greedy Soupの順位づけ・同点処理・採用条件を固定。初期案はPhase 1のB64・3 seedsのepoch 50を再利用。
- [ ] U-02: Uniform/Greedy Soupとlogit ensembleの評価を実装し、同一モデルの平均、候補の互換性、整数buffer、test非使用の選択処理を検証。
- [ ] U-03: 候補間のbarrier、単体モデル、Soup、ensembleを評価し、複数軌跡・Soup形成の動画と評価表を保存。Soup自体も実モデルとして評価する。

現時点の候補: Greedy Soupはvalidation accuracyの厳密な改善のみ採用し、同率の候補はrun ID順。学習率違いの追加runは、この最小版とは分けて判断する。

完了条件: 候補・採否・平均方法・評価結果が記録され、重み平均と予測平均が混同されていない。

## 8. 最終評価・成果物の整理

依存: 対象実験の候補選択と設定が固定されていること。

- [ ] F-01: 固定済みのモデル・手法を公式test setで最終評価。testの結果を見て候補選択やハイパーパラメータをやり直さない。
- [ ] F-02: 動画、補間図、barrier matrix、性能表、設定、実行環境、再現手順を整理。コードの版はGitを利用する場合にcommitを記録し、未管理ならその事実を明記。
- [ ] F-03: 観察と解釈を分けて記述し、2D射影・有限点補間・seed数・subset・数値精度の限界を明記。参照した手法の出典を付ける。

## 初版の対象外・後で判断すること

- 更新単位の高密度な軌跡記録と、勾配ノイズの因果効果を調べる対照実験。
- 曲線を学習するmode connectivity、Hessianやsharpnessの詳細な評価。
- filter-normalized方向による追加可視化、独立した3D/対話UI。
- 追加モデル・追加データセット・追加ハイパーパラメータ群・分散学習・DINO自体の事前学習。

これらは自動で着手せず、必要性と予算を確認してからリストへ追加します。
